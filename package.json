{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.1.8"
  },
  "scripts": {
    "start:app": "lerna exec --scope app -- yarn run dev",
    "start:components": "lerna exec --scope components -- yarn run dev",
    "build:app": "lerna exec --scope app -- yarn build",
    "build:components": "lerna exec --scope components -- yarn build"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  }
}
